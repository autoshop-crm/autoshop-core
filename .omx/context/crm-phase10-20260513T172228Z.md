# CRM Phase 10 Context Snapshot
- Task statement: Execute Phase 10 from `ContextProject/Now/CRM_CORE_FULL_CRM_DEVELOPMENT_PLAN.md` using all approved and implemented CRM phases as binding contracts.
- Desired outcome: production-readiness hardening for migration/backfill validation, old-consumer compatibility, must-pass verification matrix, observability/audit coverage, and rollout cleanup/rollback outputs.
- Known facts/evidence:
  - Phases 0, 1A, 1B artifacts exist under `ContextProject/Now/`.
  - Phases 2-9 code is implemented in the working tree.
  - Full `./gradlew test` and `./gradlew build -x test` passed at end of Phase 8/9.
  - Timeline persistence exists via `order_timeline_entry`; approval and parts lifecycle tables exist.
  - Current `OrderResponseDTO.status` exposes CRM states directly; no explicit legacy compatibility projection found yet.
- Constraints:
  - Phase 0 compatibility and non-fabrication policy are binding.
  - One order equals one invoice; idempotency and audit correctness must hold.
  - Do not revert unrelated dirty files from earlier phases.
- Unknowns/open questions:
  - Whether a dedicated legacy compatibility shim already exists elsewhere.
  - Whether historical backfill is implemented or only documented.
  - Which exact scenarios still need explicit verification artifacts.
- Likely codebase touchpoints:
  - `src/main/java/com/vladko/autoshopcore/order/**`
  - `src/main/java/com/vladko/autoshopcore/order/timeline/**`
  - `src/main/java/com/vladko/autoshopcore/event/notification/**`
  - `src/main/resources/db/changelog/**`
  - `src/test/java/com/vladko/autoshopcore/order/**`
  - `ContextProject/Now/**`
