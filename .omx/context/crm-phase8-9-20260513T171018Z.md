# Context Snapshot
- task statement: Execute Phases 8 and 9 from CRM core plan using all previously approved CRM artifacts as binding contracts.
- desired outcome: Implement loyalty operational configuration, loyalty-enabled/disabled behavior seams, CRM-safe read models, operational history queries, queue summaries, search/filter semantics, and retention/paging-ready query behavior with strict verification.
- known facts/evidence: Phases 2-7 are implemented locally with booking, approvals, waiting-for-part, role checks, customer timeline, and notifications. Loyalty module already exists but has no global enabled/disabled switch. Order read APIs are still mostly primitive lists by customer/vehicle/status.
- constraints: Preserve compatibility, additive schema, loyalty-off must not break lifecycle, read models should be operational and CRM-safe rather than BI-heavy.
- unknowns/open questions: Existing app config conventions for feature flags/settings and whether retention/paging should be explicit DTO params now or modeled through simple pageable-like parameters.
- likely codebase touchpoints: loyalty/**, order/**, timeline/**, configuration/**, Liquibase changelogs, tests, CRM docs.
